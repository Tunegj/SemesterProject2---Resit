# SemesterProject2---Resit

## AI LOG

17.06.2026: Couldn't get the API to work, so used ChatGpt to find the error.
outcome: I had the wrong URL in the .env file, and also I had a typo in the id of the element I was testing with. After fixing these, the API calls worked as expected.

20.06.2026: Problem shooting why my Tailwind CSS wasn't applying on my live site.
Outcome: I applied "style" to the iconify icons, and that fixed the problem. I also had some typos in my CSS and I had to change the path to my CSS file in main.ts. After fixing these, the styles applied correctly on the live site.
But the logo would still not position like I wanted it to, so I had to change the HTML stucture and add some regular CSS to get it to work - same with hover on the links.
